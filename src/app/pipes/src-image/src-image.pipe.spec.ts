import { SecurePipe } from './src-image.pipe';

describe('secure', () => {
  // it('create an instance', () => {
  //   const pipe = new SrcImagePipe('');
  //   expect(pipe).toBeTruthy();
  // });
});
